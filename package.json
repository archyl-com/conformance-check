{
  "name": "@archyl/conformance-check-action",
  "version": "1.0.0",
  "private": true,
  "description": "Run architecture conformance rules against changed files in CI",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@actions/http-client": "^2.2.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
